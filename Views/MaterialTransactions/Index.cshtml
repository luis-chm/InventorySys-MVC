@model IEnumerable<InventorySys.Models.TblMaterialTransaction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaterialTransactionType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaterialTransactionQuantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaterialTransactionDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Material)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaterialTransactionType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaterialTransactionQuantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaterialTransactionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Material.MaterialId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaterialTransactionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaterialTransactionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaterialTransactionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
