@model IEnumerable<InventorySys.Models.TblFiniture>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FinitureCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinitureName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinitureActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FinitureCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FinitureName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FinitureActive)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FinitureId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FinitureId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FinitureId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
